name: Build and Publish Docker Image

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  packages: write
  id-token: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile
          push: true
          platforms: linux/amd64,linux/arm64
          tags: |
            ghcr.io/${{ github.repository_owner }}/toxpredict:latest
            ghcr.io/${{ github.repository_owner }}/toxpredict:${{ github.sha }}

# Optional: add a step to publish to Docker Hub (requires DOCKERHUB_USERNAME and DOCKERHUB_TOKEN secrets)
#      - name: Log in to Docker Hub
#        if: ${{ secrets.DOCKERHUB_TOKEN }}
#        uses: docker/login-action@v2
#        with:
#          username: ${{ secrets.DOCKERHUB_USERNAME }}
#          password: ${{ secrets.DOCKERHUB_TOKEN }}
#
#      - name: Push to Docker Hub
#        if: ${{ secrets.DOCKERHUB_TOKEN }}
#        uses: docker/build-push-action@v5
#        with:
#          context: .
#          file: Dockerfile
#          push: true
#          tags: |
#            ${{ secrets.DOCKERHUB_USERNAME }}/toxpredict:latest
#            ${{ secrets.DOCKERHUB_USERNAME }}/toxpredict:${{ github.sha }}
